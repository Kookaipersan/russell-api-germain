<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Modifier Réservation</title>
</head>
<body>
  <%- include('../component/header') %>

  <h2>Modifier une Réservation</h2>

  <% if (errorMessage) { %>
    <p style="color:red;"><%= errorMessage %></p>
  <% } %>

  <form action="/reservations/edit/<%= reservation._id %>" method="POST">
    <label>Numéro de Catway :</label>
    <input type="text" name="catwayNumber" value="<%= reservation.catwayNumber %>" required><br>

    <label>Nom du Client :</label>
    <input type="text" name="clientName" value="<%= reservation.clientName %>" required><br>

    <label>Nom du Bateau :</label>
    <input type="text" name="boatName" value="<%= reservation.boatName %>" required><br>

    <label>Date de début :</label>
    <input type="date" name="startDate" value="<%= reservation.startDate.toISOString().split('T')[0] %>" required><br>

    <label>Date de fin :</label>
    <input type="date" name="endDate" value="<%= reservation.endDate.toISOString().split('T')[0] %>" required><br>

    <button type="submit">Enregistrer</button>
  </form>

  <a href="/reservations/list">← Retour à la liste</a>
</body>
</html>